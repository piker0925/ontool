spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  security:
    user:
      name: ${ADMIN_USERNAME}
      password: ${ADMIN_PASSWORD}

cors:
  origin: ${CORS_ORIGIN:http://localhost:5173}

storage:
  base-url: ${STORAGE_BASE_URL}
  upload-dir: uploads
  # 결과 파일 보존 시간. 만료된 Job의 결과·입력 파일과 고아 파일 청소 기준.
  result-ttl: PT1H

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: never
